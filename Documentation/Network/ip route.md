---
date: "2024-08-04"
name: ip route
time: 17:01
---

# ip route

```bash
ip route add 10.10.10.0/24 dev [device name] # set the route for ip that is outside of systems's network
ip route add default dev [device name] # set the default route
```
