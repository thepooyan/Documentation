```bash
docker run imageName //if not present will pull from hub
docker stop [container name OR id]
docker ps //list all running containers
coker ps -a // shows even stopped containers
docker remove //remove a container for good
docker images //list images
docker rmi imageName //delete an image

docker pull ubunto //pull the image
```