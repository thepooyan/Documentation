```cli
sudo -s		/become the super user
exit		/exit the super user
sudo -		/become the super user (not recommended version)??

su - cindy	/become another user named cindy
su cindy	/becomes the other user but in the current environment?

users		/list all the users that are logged in 
id		/show a bunch of stuff about the user himself
```